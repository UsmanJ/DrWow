extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    h1 You are currently logged in as #{user.username}
    a(href="/logout") Logout
